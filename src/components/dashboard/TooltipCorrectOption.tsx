import { FaCheckCircle } from "react-icons/fa";

export default function TooltipCorrectOption() {
	return (
		<FaCheckCircle className="absolute -right-1 -top-1 rounded-full bg-white text-green-600" />
	);
}
